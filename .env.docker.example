# ==================================================================
# EXAMZONE DOCKER ENVIRONMENT CONFIGURATION
# ==================================================================
# This file contains environment variables for running ExamZone with Docker
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control
# ==================================================================

# ==================================================================
# DOCKER CONFIGURATION
# ==================================================================
# Node environment (development | production)
NODE_ENV=development

# Port mappings (host:container)
BACKEND_PORT=3000
AI_GENERATOR_PORT=5001
AI_CHECKER_PORT=5002

# Docker registry for production (Azure Container Registry)
# Example: examzoneregistry.azurecr.io
DOCKER_REGISTRY=your-registry.azurecr.io

# Image tag (use commit SHA or version number in production)
IMAGE_TAG=latest

# ==================================================================
# DATABASE CONFIGURATION (REQUIRED)
# ==================================================================
# MongoDB Atlas connection string (recommended for production)
# Format: mongodb+srv://username:password@cluster.mongodb.net/examzone?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/examzone

# ==================================================================
# AUTHENTICATION & SECURITY (REQUIRED)
# ==================================================================
# JWT secret key - use a strong random string (min 32 chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Session secret for Express sessions
SESSION_SECRET=your-session-secret-change-this-in-production-min-32-chars

# JWT token expiration (default: 7d)
JWT_EXPIRES_IN=7d

# ==================================================================
# GOOGLE OAUTH (OPTIONAL)
# ==================================================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ==================================================================
# EMAIL CONFIGURATION (OPTIONAL)
# ==================================================================
# SMTP configuration for sending emails
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# ==================================================================
# AI SERVICES CONFIGURATION (REQUIRED)
# ==================================================================
# Google Gemini API Key for AI services
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# AI Service URLs (for production cloud deployment)
# For local Docker: use container names (http://ai-question-generator:5001)
# For cloud: use actual service URLs
AI_QUESTION_GENERATOR_URL=http://ai-question-generator:5001
AI_ANSWER_CHECKER_URL=http://ai-answer-checker:5002

# ==================================================================
# FRONTEND CONFIGURATION (REQUIRED)
# ==================================================================
# Frontend URL for CORS configuration
# Local: http://localhost:5173
# Production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:5173

# ==================================================================
# CLOUD DEPLOYMENT (AZURE PRODUCTION)
# ==================================================================
# These variables are used when deploying to Azure Container Apps or App Service
# Azure Resource Group
# AZURE_RESOURCE_GROUP=examzone-rg

# Azure Container Registry name
# ACR_NAME=examzoneregistry

# Azure location
# AZURE_LOCATION=eastus

# Azure subscription ID
# AZURE_SUBSCRIPTION_ID=your-subscription-id
